// built in rules
WHITESPACE = _{ " " | "\t" | "\r" | "\n" }
COMMENT = _{ silent_comment }

// helper rules
silent_comment = @{ "#" ~ (!"\n" ~ ANY)* ~ "\n" }

// smali content
smali_file = {
    ".class"
}

// parser root
root = {
    SOI
    ~ smali_file
    ~ EOI
}