// built in rules
WHITESPACE = _{ " " | "\t" | "\r" | "\n" }
COMMENT = _{ h_comment }

// helper rules
h_comment = @{ "#" ~ (!"\n" ~ ANY)* ~ "\n" }

// smali content
s_file = {
    ".class"
}

// parser root
entry = _{
    SOI
    ~ s_file
    ~ EOI
}